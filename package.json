{
  "name": "flashbang",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "bun scripts/build.ts",
    "build:sw": "bun build src/sw/sw.ts --outdir dist --entry-naming sw.js --minify --target browser",
    "build:ui": "bun build src/ui/app.ts --outdir dist --entry-naming app.js --minify --target browser",
    "build:css": "bunx unocss 'src/ui/index.html' 'src/ui/app.ts' 'src/ui/liquid-metal.ts' -o dist/styles.css --minify",
    "dev": "bun --watch scripts/dev.ts",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@minify-html/node": "^0.18.1",
    "@types/bun": "latest",
    "@unocss/cli": "^66.6.0",
    "typescript": "^5.9.3",
    "unocss": "^66.6.0"
  },
  "dependencies": {}
}
